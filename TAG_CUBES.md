# 03、04 AprilTag 方块中心 6D 位姿

本程序当前使用 **MV-CH120-10GM / DB1488042、12 mm 镜头**，相机实际取流为 **4096×3000**。沿用已打印贴纸的 tag36h11 编号规则：03 方块为 **ID 10–14**，04 方块为 **ID 15–19**；每组依次为中心、左、右、上、下面。方块边长 **16 mm**，Tag 黑色外框边长 **12.8 mm**。

程序同时显示两个方块中心相对相机的 XYZ、RPY、三轴与立方体框。可以在画面里点选父 Link 和子 Link，分别定格初始帧与结束帧，计算子块相对父块的旋转角幅值。支持原图/标注图/JSON 快照和逐帧 JSONL、CSV 记录。

程序、标定和测量记录统一放在 `/home/tan/HK`，无需 ROS 或工作空间安装。启动脚本可从任意目录执行；默认内参为 `HK/calibration_results/`，测量结果写入 `HK/cube_pose_runs/`。可通过 `HK_PYTHON` 指定 Python 解释器。迁移前的历史 JSON 保留采集时的路径和哈希，原始数据未改写。

## 先标定当前相机

```bash
bash ~/HK/scripts/tag_cubes.sh --calibrate
```

已按用户确认设置 **12×9 个格子、11×8 个内角点、格宽 15 mm**，默认选择序列号 DB1488042，相机名为 `CH120-10GM-1`。界面自动连接并开始取流；相机曝光等设置在“相机参数”页。

1. 在实际工作距离调清晰对焦与光圈，然后开始采样；采样中调整镜头，应清空旧样本重采。
2. 保持完整棋盘格可见，采集至少 12 张、建议 25 张位置/距离/倾斜角不同的清晰图像，覆盖画面中心和四周。自动采样会检查清晰度与视角变化。
3. 点击“标定并保存内参”，默认文件名带 `CH120-10GM-1`，同时生成 JSON 结果与样本记录。
4. 关闭标定窗口或断开相机，然后启动方块检测。保持标定时的镜头、对焦与图像几何设置。

**12 mm 只是镜头标称焦距，程序不会据此猜测 K。** 未找到本相机内参时只显示图像和 ID，数值状态为 `UNCALIBRATED`。分辨率不匹配时不输出米制位姿。

## 实时检测

```bash
bash ~/HK/scripts/tag_cubes.sh
```

默认内参路径与标定程序一致；若另存了其他文件，点击“加载这台相机的内参”，或使用 `--intrinsics /绝对路径/相机内参.yaml`。

相机连接、曝光、标定、普通快照和连续记录按钮位于右侧 **“相机 / 标定 / 记录”** 页。

- 图像中黄色轮廓表示检出的 Tag；绿色立方体框与红、绿、蓝轴表示当前有效的方块姿态。
- 方块原点为**立方体中心**，+X 指向留空安装面；+Y 指向十字贴纸左臂，+Z 指向上臂。五个面的方向与已生成的贴纸一致。
- 相机坐标为 **+X 向右、+Y 向下、+Z 向前**。`T_camera_cube` 的含义为 `p_camera = R @ p_cube + t`。
- 平移显示 mm；JSON 同时保存 m、mm、旋转矩阵及 `xyzw` 四元数。RPY 是固定轴 XYZ，`R = Rz(yaw) @ Ry(pitch) @ Rx(roll)`，单位度。接近万向锁时可使用四元数。
- JSON 通用字段 `cube_pair_relative` 当前表示 `inverse(T_camera_cube03) @ T_camera_cube04`，并明确记录 `from_cube: 3`、`to_cube: 4`；只在两块同一帧均有有效位姿时计算。旧的 `cube01_T_cube02` 字段仅为 01/02 历史数据保留。界面中的相对位姿跟随实际选择的父／子方块。
- “保存原图、叠加图和当前位姿”写入 `cube_pose_runs/snapshot_时间/`。“开始记录”写入带时间戳的 JSONL 与 CSV，停止或关闭时结束。
- “停止并释放相机”可让标定/MVS程序使用相机；重新连接时重新读取内参。画面新鲜度会按实时处理帧率动态判断（完成帧允许约 0.8–1.25 秒），停止或真正过期后清除当前位姿数值。

可调整曝光，并通过命令行设置实测几何，例如 `--cube-side-mm 16 --tag-size-mm 12.8`；这里的 Tag 尺寸是黑色外框，不能填十字贴纸单面的 16 mm，也不能填打印补偿 PDF 中预拉伸的尺寸。`--lens-mm 12` 只写入采集记录。

## 点选父／子 Link，测量两帧间的转角

1. 在 **“父 / 子 Link 转角”** 页，点击“① 点选父 Link”，再点击画面中父方块的 Tag 区域。任一可见面均代表整个方块，选择后标为青色 `PARENT`。
2. 程序自动进入子块选择；点击另一方块的 Tag 区域，标为紫色 `CHILD`。也可用“② 点选子 Link”重新选择；“交换父 / 子”可直接调换两个角色。
3. 保持两块清晰、静止且同时有有效位姿，点击 **“③ 定格初始帧（设为零位）”**。画面保持在该帧，初始图像和位姿自动保存。
4. 点击 **“恢复实时画面 → 手动移动子 Link”**，手动转动子 Link，到达目标后停稳。
5. 点击 **“④ 定格结束帧 / 计算总旋转角度”**。界面保持结束帧，突出显示子 Link 相对父 Link 从初始姿态到结束姿态的整体三维转角幅值，不再显示旋转轴与位移。该标量保存在 JSON 的 `measurement.angle_deg`；计算不需要指定旋转轴，也不是三个欧拉角的相加。

“查看初始帧 / 查看结束帧”可回看已采集画面。定格时后台继续取流，但历史帧保持不变；必须恢复实时后才能采集下一帧。恢复实时后可以再次定格结束帧，沿用同一个初始零位；再次定格初始帧会新建一次测量。更换父／子选择、交换角色、加载内参或重新连接相机会清空当前基准，已保存文件保留。“重置测量”保留父／子选择。

每次初始采集新建目录 `cube_pose_runs/joint_angle_时间/`：

```text
measurement.json         # 最新一组测量、角色、两帧数据、角度和快照路径
initial/                 # 初始帧 raw.png、detected.png、poses.json、measurement.json
final_时间/               # 每次结束帧的同样四个文件；重复采集结束帧不覆盖旧帧
```

必须在**每个采集帧内同时看到父、子两块并得到有效位姿**。缺少内参、分辨率不符、丢码、歧义、重复 ID、同一帧重复采集，或实时画面超过动态新鲜度阈值仍未更新，都会拒绝此次采集。含有效单面估计时会注明，建议两块各自露出相邻两面。

计算使用旋转矩阵，不相减 RPY：

```text
T0 = inverse(T_camera_parent_initial) @ T_camera_child_initial
T1 = inverse(T_camera_parent_final)   @ T_camera_child_final
R_delta = R(T1) @ transpose(R(T0))
angle = rotation_vector_norm(R_delta)     # 转为度，范围 0–180°
```

父块在两次拍摄间的整体运动会被抵消。只要两个方块分别与父、子 Link **保持刚性固定**，安装方向可以不同；固定安装旋转只会改变转轴的坐标表示，不改变此转角幅值。对于固定单轴关节，两个关节位置之差不超过 180° 时，该值等于关节转角的绝对值。一般三维运动时，它表示两端相对朝向之间的最短旋转。

仅凭两帧不能确定顺／逆转的符号或累计圈数，例如实际转过 270° 会得到 90°。接近零转角时轴方向不稳定；零角不输出轴方向，180° 时轴的正负等价。初始帧设零是本次相对零位，不是机械绝对零位。界面显示三位小数仅是数值格式，不是物理精度承诺。

## 求解与状态

程序直接使用各面 **16 mm 方块坐标系中的三维角点**联合拟合，将可见面约束到同一个方块中心，不使用旧单码工具的坐标轴重映射。单面使用正方形 IPPE 候选与独立平面初始化，多面增加 SQPnP 初值；在当前帧按整面重投影一致性选取观测，再用 LM 优化。不使用前一帧、平滑或限速掩盖真实运动。[OpenCV PnP 方法与点序说明](https://docs.opencv.org/4.12.0/d5/d1f/calib3d_solvePnP.html)。

| 状态 | 意义 |
|---|---|
| `OK_MULTI_FACE` | 至少两个面通过检查，输出有效位姿 |
| `OK_SINGLE_FACE` | 当前只有一面可用，输出单面估计；应优先让相邻两面可见 |
| `AMBIGUOUS` | 存在明显不同、重投影误差又接近的候选；不作为有效位姿 |
| `INCONSISTENT_FACES` | 检出了多个面，但无法找到至少两个一致的面 |
| `DUPLICATE_ID` | 同一方块的同一 ID 在一帧出现多次，可能混入备用贴纸 |
| `NOT_VISIBLE` | 该方块当前没有检测结果，不沿用旧位姿 |
| `UNCALIBRATED` / `IMAGE_SIZE_MISMATCH` | 缺少内参或图像尺寸不符 |
| `POSE_FAILED` | 当前角点无法得到通过几何检查的姿态 |

默认整面最大角点误差阈值为 3 px；候选相差大于 3° 或 0.5 mm，且 RMS 差小于 0.15 px 时标记歧义。这些是程序筛选阈值，不是实测精度保证。JSON 的 `pose_valid` 才表示当前结果可用；无效状态可能保留最佳候选用于诊断，CSV 数值列与界面不输出该候选。

## 离线图像与接口

```bash
bash ~/HK/scripts/tag_cubes.sh \
  --image /path/to/image.png --intrinsics /path/to/intrinsics.yaml

# 有限帧数、无界面；输出目录和记录文件应是新路径。
bash ~/HK/scripts/tag_cubes.sh \
  --headless --frames 100 --record /tmp/cubes_run01.jsonl --output /tmp/cubes_snapshot01
```

也支持 `--video`。记录包含主机接收时间、相机帧编号、原始检测角点、采用/剔除 ID、内参文件哈希、重投影误差及完整姿态。主机时间不是曝光时间；这里尚未实现多相机同步或 ROS TF 发布。

源码：[核心求解](cube_pose_core.py)、[界面和采集](cube_pose_app.py)、[两帧角度计算](cube_joint_measurement.py)、[回归检查](tests/test_cube_pose.py)、[角度检查](tests/test_cube_joint_measurement.py)、[界面操作检查](tests/test_cube_joint_gui.py)。

## 本次验证范围

- MVS SDK 实际发现 MV-CU013-A0UC / DB0447679；新程序已通过 8 帧实机取流、最新帧策略与 JSONL/CSV 保存检查，图像为 1280×1024。该次画面没有检出01、02方块，也未加载标定，未输出米制位姿。
- 无相机测试覆盖五个面及 90° 图案旋转、多面与相对位姿、异常面、重复 ID、缺少标定、分辨率不符、丢失观测和单面歧义。默认运行环境为 `revo3_ros` / OpenCV 4.11；同时检查系统 OpenCV 4.6 兼容路径。
- 从已打印贴纸的坐标文件渲染两块测试图，通过真实 AprilTag 解码、PnP、界面和保存链路。该合成样本的方块中心误差约 0.175/0.179 mm，旋转误差约 0.042/0.084°；这些数字仅验证软件链路，不代表物理相机精度。[合成验证记录](cube_pose_runs/synthetic_validation/validation.json)、[界面示例](cube_pose_runs/synthetic_validation/gui.png)。
- 当前相机内参已于 2026-09-17 17:06（北京时间）保存到默认路径，尺寸为 1280×1024。内参文件存在不等于实物角度精度已验证，仍需使用已知角度的转台／关节实测。
- 本次转角功能完成后，使用这份内参通过 12 帧真实相机取流检查。保存帧中 01 为 `OK_MULTI_FACE`（ID 2、0），02 为 `OK_SINGLE_FACE`（ID 9），两块均输出有效位姿；02 仍建议调整到相邻两面可见。[实机检查帧](cube_pose_runs/joint_angle_validation/live_camera_check/poses.json)。未手动操作实物关节做已知角度验证。
- 转角验证覆盖随机安装方向、父块共同运动、纯平移、0°/180°、大于 180° 的主值、选择与标定变化、歧义与缺失观测。合成两帧中设定子块相对转动 30°，同时改变父块位姿；实际解码、PnP 和 Qt 点选定格流程得到约 **29.943°**，并验证保存、回看、交换角色和过期画面拒绝。[软件流程验证记录](cube_pose_runs/joint_angle_validation/validation.json)、[界面示例](cube_pose_runs/joint_angle_validation/gui.png)。这不是实物精度测量。
